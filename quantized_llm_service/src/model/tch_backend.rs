//! Tch backend specific utilities live here.
//! The current implementation relies directly on TorchScript modules exported via
//! `scripts/prepare_model.py`, so no additional helpers are required yet.
